import { createHash } from "crypto"; import { mkdirSync, readFileSync, writeFileSync } from "fs"; import { resolve } from "path"; import { getHeapStatistics } from "v8"; import { Wt0Dataset } from "./wt0-contract"; import { buildWt1, validateWt1 } from "./wt1-catalog";
const hash=(value:string|Buffer)=>createHash("sha256").update(value).digest("hex"),root=resolve(process.cwd());if(getHeapStatistics().heap_size_limit>=1024*1024*1024)throw new Error("WT1 requires a Node heap below 1 GiB");const read=(name:string)=>readFileSync(resolve(root,"data/database-world-tournament-captures/wt0",name),"utf8"),wt0Text=read("wt0-inventory.json"),wt0ValidationText=read("wt0-validation.json"),wt0Manifest=JSON.parse(read("wt0-manifest.json")) as {members:Array<{fileName:string;sizeBytes:number;sha256:string}>};for(const [name,text]of [["wt0-inventory.json",wt0Text],["wt0-validation.json",wt0ValidationText]]as const){const member=wt0Manifest.members?.find(value=>value.fileName===name);if(!member||member.sizeBytes!==Buffer.byteLength(text)||member.sha256!==hash(text))throw new Error("WT1 WT0 manifest mismatch")}if(JSON.parse(wt0ValidationText)?.valid!==true)throw new Error("WT1 WT0 validation not green");const dataset=buildWt1(JSON.parse(wt0Text)as Wt0Dataset,wt0Text),validation=validateWt1(dataset),payload=`${JSON.stringify(dataset,null,2)}\n`,validationText=`${JSON.stringify(validation,null,2)}\n`,output=resolve(root,"data/database-world-tournament-captures/wt1"),member=(fileName:string,text:string)=>({fileName,sizeBytes:Buffer.byteLength(text),sha256:hash(text)});mkdirSync(output,{recursive:true});const payloadPath=resolve(output,"wt1-route-catalog.json"),validationPath=resolve(output,"wt1-validation.json");writeFileSync(payloadPath,payload);writeFileSync(validationPath,validationText);if(hash(readFileSync(payloadPath))!==hash(payload)||hash(readFileSync(validationPath))!==hash(validationText))throw new Error("WT1 post-write mismatch");writeFileSync(resolve(output,"wt1-manifest.json"),`${JSON.stringify({schemaVersion:1,contractVersion:dataset.contractVersion,members:[member("wt1-route-catalog.json",payload),member("wt1-validation.json",validationText)]},null,2)}\n`);process.stdout.write(`${JSON.stringify(validation)}\n`);
