# Database Team Analysis experiment v22 (DB23)

Status: experimental, non-production. Contract version: `0.22.0`.

DB23 recomputes every residual attribution from the final DB22 AST-normalized parity view. DB18 and DB22 overrides are accepted only when their before-views reproduce the preceding source view exactly. Multiset occurrences remain distinct, and attribution precedence remains occurrence imbalance, logical context, polarity, comparator, threshold, then absence.

The gate is diagnostic. It inherits three native semantic promotions, introduces none, and establishes the current frontier for selecting new first-party investigations without carrying stale DB19 classifications.
