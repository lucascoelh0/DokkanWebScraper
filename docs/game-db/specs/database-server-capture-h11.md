# Database/server capture H11 — extension shadow parity

H11 pins H7–H10 bytes and compares H10 definition-only facts conservatively with H3–H6. H7 supplies the already-reviewed H0–H7, S0–S7 and selected E0–E9 closure. E gates not used as direct inputs remain non-comparable, not silently agreeing. No K0–K9 artifact exists in the allowlisted local data roots, so K remains unavailable rather than invented.

Every cell is agreement, representation gain, confirmed conflict, unknown or unjoinable. Totals are non-exclusive: per-HAR facts overlap and different units are not interchangeable. Numeric equality is accepted only through an explicit field/domain mapping; ambiguous IDs remain unjoinable. Old-only coverage is unknown. Zero confirmed conflicts never means complete parity.
