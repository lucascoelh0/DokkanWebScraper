import { mkdirSync, writeFileSync } from "fs"; import { resolve } from "path"; import { loadDdContext } from "./data-download-context"; import { buildDd5, validateDd5 } from "./data-download-dd5";
const { root, loaded, dd1, dd2 } = loadDdContext(), dataset = buildDd5(loaded, dd1, dd2), validation = validateDd5(dataset), out = resolve(root, "data/database-data-download-captures/dd5"); mkdirSync(out, { recursive: true }); writeFileSync(resolve(out, "data-download-dd5-cache-integrity.json"), `${JSON.stringify(dataset, null, 2)}\n`); writeFileSync(resolve(out, "data-download-dd5-validation.json"), `${JSON.stringify(validation, null, 2)}\n`); process.stdout.write(`${JSON.stringify(validation)}\n`);
