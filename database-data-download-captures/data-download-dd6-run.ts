import { mkdirSync, readFileSync, writeFileSync } from "fs"; import { resolve } from "path"; import { loadDdContext } from "./data-download-context"; import { buildDd6, Dd6SourceLock, validateDd6 } from "./data-download-dd6";
async function main() { const { root, dd2 } = loadDdContext(), lock = JSON.parse(readFileSync(resolve(root, "database-data-download-captures/data-download-dd6-source-lock.json"), "utf8")) as Dd6SourceLock, dataset = await buildDd6(root, lock, dd2), validation = validateDd6(dataset), out = resolve(root, "data/database-data-download-captures/dd6"); mkdirSync(out, { recursive: true }); writeFileSync(resolve(out, "data-download-dd6-shadow-parity.json"), `${JSON.stringify(dataset, null, 2)}\n`); writeFileSync(resolve(out, "data-download-dd6-validation.json"), `${JSON.stringify(validation, null, 2)}\n`); process.stdout.write(`${JSON.stringify(validation)}\n`); } main().catch(error => { console.error(error instanceof Error ? error.message : "DD6 failed"); process.exitCode = 1; });
