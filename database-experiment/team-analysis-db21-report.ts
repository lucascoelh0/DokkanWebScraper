import { DatabaseTeamAnalysisDb21Coverage, DatabaseTeamAnalysisDb21Dataset } from "./team-analysis-db21-contract";
export function renderDatabaseTeamAnalysisDb21Report(dataset: DatabaseTeamAnalysisDb21Dataset, coverage: DatabaseTeamAnalysisDb21Coverage): string { const rows = dataset.conclusions.map(value => `| \`${value.semantic}\` | \`${value.status}\` |`).join("\n"); return `# Database Team Analysis experiment — DB21 passive turn native audit\n\nDB21 verifies the identity of six relevant native symbols and records the bounded linkage audit. Loading and timing-related runtime APIs exist, but no field-to-runtime linkage for \`passive_skills.turn\` or \`is_once\` was proved.\n\n- Audited DB20 candidates: **${coverage.auditedCandidateCount}** (${coverage.exactNumericMatchCount} exact numeric matches; ${coverage.numericMismatchCount} mismatches).\n- Verified native symbols: **${coverage.verifiedSymbolCount}**.\n- Linkage status: **${coverage.linkageStatus}**.\n- Inherited/new semantic promotions: **3/0**.\n\n| Semantic | Status |\n|---|---|\n${rows}\n\nThe audit is bounded and does not claim exhaustive absence of an inlined or indirect consumer. It does prove that the inspected symbols do not establish the required linkage. No DB20 correlation is promoted, and production replacement remains NO-GO.\n`; }
