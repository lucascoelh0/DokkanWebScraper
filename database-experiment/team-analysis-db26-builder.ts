import { createHash } from "crypto";
import { DatabaseExperimentTables } from "./builder";
import { SqliteScalar } from "./contract";
import { NativeRuntimeElfInspection } from "./native-runtime-elf-adapter";
import { Db11ConditionExpression, DatabaseTeamAnalysisDb11Dataset } from "./team-analysis-db11-contract";
import { DatabaseTeamAnalysisDb8Dataset } from "./team-analysis-db8-contract";
import { DatabaseTeamAnalysisDb9Dataset } from "./team-analysis-db9-contract";
import { DatabaseTeamAnalysisDb26Coverage, DatabaseTeamAnalysisDb26Dataset, Db26NativeSpecialCategoryEvidence, Db26SpecialCategoryResolution } from "./team-analysis-db26-contract";
const OBSERVATIONS = new Map([["causality_handler", "additional_param_gate_then_low8_category_attribute_and_cau_val1_bitwise_intersection"], ["category_row_constructor", "raw_attribute_loaded_masked_to_low8_and_stored_at_offset8"], ["category_attribute_getter", "attribute_loaded_from_offset8"], ["card_special_view_category_getter", "special_view_category_id_resolved_to_category_object"]]);
const CONCLUSIONS = ["cau_val1_is_low8_special_category_attribute_mask", "predicate_requires_nonempty_mask_intersection", "special_category_raw_attribute_is_structured_sqlite_identity"];
const UNKNOWNS = ["event_direction", "attacker_target_role", "additional_param_gate_meaning", "activation_timing", "recurrence", "calculation_bucket"];
function sha256(value: Buffer): string { return createHash("sha256").update(value).digest("hex"); }
function integer(value: unknown): number | undefined { const parsed = typeof value === "number" ? value : typeof value === "string" && value.trim() ? Number(value) : NaN; return Number.isSafeInteger(parsed) ? parsed : undefined; }
function validateEvidence(inspection: NativeRuntimeElfInspection, evidence: Db26NativeSpecialCategoryEvidence, nativeSha256: string): void {
    if (evidence.schemaVersion !== 1 || evidence.sourceSha256 !== nativeSha256 || evidence.causalityType !== 49 || evidence.auditScope !== "skill-causality-mask-to-card-special-category-attribute" || JSON.stringify(evidence.conclusions) !== JSON.stringify(CONCLUSIONS) || JSON.stringify(evidence.unknowns) !== JSON.stringify(UNKNOWNS) || JSON.stringify(evidence.parameterReads) !== JSON.stringify(["cau_val1"]) || JSON.stringify(evidence.ignoredParameters) !== JSON.stringify(["cau_val2", "cau_val3"])) throw new Error("DB26 native evidence identity mismatch");
    if (evidence.sqliteBinding.table !== "special_categories" || evidence.sqliteBinding.column !== "raw_attribute" || evidence.sqliteBinding.objectOffset !== 8 || evidence.sqliteBinding.storedWidthBits !== 8 || !inspection.readVirtualBytes(evidence.sqliteBinding.literalVma, 14).equals(Buffer.from("raw_attribute\0"))) throw new Error("DB26 SQLite binding mismatch");
    if (evidence.additionalParamGate.byteOffset !== 1 || evidence.additionalParamGate.bit !== 0 || evidence.additionalParamGate.predicate !== "bit_set") throw new Error("DB26 AdditionalParam gate evidence mismatch");
    if (evidence.codeRegions.length !== OBSERVATIONS.size || new Set(evidence.codeRegions.map(value => value.role)).size !== OBSERVATIONS.size) throw new Error("DB26 native code region cardinality mismatch");
    for (const region of evidence.codeRegions) { const symbol = inspection.symbols.find(value => value.name === region.symbol); if (OBSERVATIONS.get(region.role) !== region.observation || !symbol || symbol.value !== region.vma || symbol.size !== region.sizeBytes || sha256(inspection.readVirtualBytes(region.vma, region.sizeBytes)) !== region.codeSha256) throw new Error(`DB26 invalid native code region ${region.role}`); }
}
function collect(expression: Db11ConditionExpression): string[] { if (expression.op === "unknown" && integer(expression.causalityType ?? (typeof expression.raw === "object" && expression.raw !== null ? (expression.raw as Record<string, unknown>).causality_type : undefined)) === 49) return [String(expression.causalityId)]; if (expression.op === "not") return collect(expression.child); if (expression.op === "all" || expression.op === "any") return expression.children.flatMap(collect); return []; }
export function buildDatabaseTeamAnalysisDb26Dataset(options: { db8: DatabaseTeamAnalysisDb8Dataset, db8Sha256: string, db9: DatabaseTeamAnalysisDb9Dataset, db9Sha256: string, db11: DatabaseTeamAnalysisDb11Dataset, db11Sha256: string, tables: DatabaseExperimentTables, inspection: NativeRuntimeElfInspection, nativeSha256: string, nativeSizeBytes: number, evidence: Db26NativeSpecialCategoryEvidence, evidenceSha256: string }): DatabaseTeamAnalysisDb26Dataset {
    if (options.db8.contractVersion !== "0.7.0" || options.db9.contractVersion !== "0.8.0" || options.db11.contractVersion !== "0.10.0" || options.db8.semanticPromotionCount !== 0 || options.db9.semanticPromotionCount !== 0 || options.db8.sourceSha256 !== options.db9.sourceDatabaseSha256 || options.db8.sourceSha256 !== options.db11.sourceSha256 || options.db8.sourceSnapshotVersion !== options.db9.sourceSnapshotVersion || options.db8.sourceSnapshotVersion !== options.db11.sourceSnapshotVersion || options.db9.sourceDb8.sha256 !== options.db8Sha256 || options.db9.nativeRuntime.sha256 !== options.nativeSha256 || options.db9.nativeRuntime.sizeBytes !== options.nativeSizeBytes) throw new Error("DB26 source lineage mismatch");
    validateEvidence(options.inspection, options.evidence, options.nativeSha256);
    const gaps = options.db8.causalityGaps.filter(value => integer(value.causalityType) === 49), runtimes = options.db9.causalityGapEvidence.filter(value => value.enumValue === 49), dispatches = options.db9.causalityDispatchSlots.filter(value => value.enumValue === 49), handler = options.evidence.codeRegions.find(value => value.role === "causality_handler")!;
    if (gaps.length !== 1 || runtimes.length !== 1 || dispatches.length !== 1) throw new Error("DB26 causality 49 evidence cardinality mismatch"); const gap = gaps[0], runtime = runtimes[0], dispatch = dispatches[0];
    if (runtime.identityStatus !== "runtime_identified" || runtime.occurrenceCount !== gap.occurrenceCount || runtime.affectedStateCount !== gap.affectedStateCount || runtime.symbol !== handler.symbol || runtime.symbolAddress !== handler.vma || dispatch.status !== "identified" || dispatch.symbol !== handler.symbol || dispatch.symbolAddress !== handler.vma) throw new Error("DB26 causality 49 runtime lineage mismatch");
    const categories: Db26SpecialCategoryResolution["selector"]["categories"] = []; const attributes = new Set<number>(), categoryIds = new Set<string>();
    for (const row of options.tables.special_categories ?? []) { if (!["id", "raw_attribute", "name"].every(column => Object.prototype.hasOwnProperty.call(row, column))) throw new Error("DB26 incomplete special_categories row"); const id = row.id === null || row.id === "" ? undefined : String(row.id), rawAttribute = integer(row.raw_attribute); if (!id || categoryIds.has(id)) throw new Error(`DB26 invalid or duplicate special category id ${row.id}`); if (rawAttribute === undefined || rawAttribute <= 0 || rawAttribute > 255 || (rawAttribute & (rawAttribute - 1)) !== 0 || attributes.has(rawAttribute)) throw new Error(`DB26 invalid or duplicate special category attribute ${row.raw_attribute}`); categoryIds.add(id); attributes.add(rawAttribute); categories.push({ firstPartyId: id, rawAttribute, localizedName: row.name === null ? null : String(row.name), provenance: { table: "special_categories", rowId: id, columns: ["id", "raw_attribute", "name"] } }); }
    categories.sort((a, b) => a.rawAttribute - b.rawAttribute || a.firstPartyId.localeCompare(b.firstPartyId, "en", { numeric: true })); if (categories.length === 0) throw new Error("DB26 special category dictionary is empty"); const knownMask = categories.reduce((value, category) => value | category.rawAttribute, 0);
    const rows = new Map<string, Record<string, SqliteScalar>>(); for (const row of options.tables.skill_causalities ?? []) { const id = String(row.id); if (rows.has(id)) throw new Error(`DB26 duplicate skill_causalities row ${id}`); rows.set(id, row); }
    const resolutions: Db26SpecialCategoryResolution[] = [], keys = new Set<string>(); for (const state of options.db11.states) for (const rule of state.passive?.rules ?? []) for (const causalityId of collect(rule.condition)) { const key = `${state.stateKey}|${rule.ruleKey}|${causalityId}`; if (keys.has(key)) throw new Error(`DB26 duplicate occurrence ${key}`); keys.add(key); const row = rows.get(causalityId); if (!row || !["causality_type", "cau_val1", "cau_val2", "cau_val3"].every(column => Object.prototype.hasOwnProperty.call(row, column)) || integer(row.causality_type) !== 49) throw new Error(`DB26 causality row missing or incomplete ${causalityId}`); const runtimeMask = integer(row.cau_val1); const supported = runtimeMask !== undefined && runtimeMask > 0 && runtimeMask <= 255 && (runtimeMask & ~knownMask) === 0; resolutions.push({ stateKey: state.stateKey, ruleKey: rule.ruleKey, passiveSkillId: rule.source.passiveSkillId, causalityId, causalityType: 49, semanticStatus: "partial", operation: "current_attack_special_category_mask_intersection", selector: { rawMask: row.cau_val1, ...(runtimeMask === undefined ? {} : { runtimeMask }), status: supported ? "supported" : "unknown", categories: supported ? categories.filter(value => (value.rawAttribute & runtimeMask!) !== 0) : [] }, activation: { additionalParamGate: "byte_1_bit_0_set", eventDirection: "unknown", attackerTargetRole: "unknown", timing: "unknown", recurrence: "unknown", calculationBucket: "unknown" }, raw: { cauVal1: row.cau_val1, cauVal2: row.cau_val2, cauVal3: row.cau_val3 }, provenance: { database: { table: "skill_causalities", rowId: causalityId, columns: ["causality_type", "cau_val1", "cau_val2", "cau_val3"] }, runtime: { fileName: "libcocos2dcpp.so", sha256: options.nativeSha256, evidenceFile: "native-special-category-semantics.json", evidenceSha256: options.evidenceSha256, codeRegions: options.evidence.codeRegions } } }); }
    resolutions.sort((a, b) => a.stateKey.localeCompare(b.stateKey, "en", { numeric: true }) || a.ruleKey.localeCompare(b.ruleKey, "en", { numeric: true }) || a.causalityId.localeCompare(b.causalityId, "en", { numeric: true })); if (resolutions.length !== gap.occurrenceCount || new Set(resolutions.map(value => value.stateKey)).size !== gap.affectedStateCount || new Set(resolutions.map(value => value.causalityId)).size !== gap.uniqueCausalityCount) throw new Error("DB26 source gap accounting mismatch");
    return { schemaVersion: 1, contract: "dokkan-team-analysis-special-category-native-semantics-experiment", contractVersion: "0.25.0", generatedAt: options.db11.generatedAt, sourceSnapshotVersion: options.db8.sourceSnapshotVersion, sourceDatabaseSha256: options.db8.sourceSha256, sourceDb8: { fileName: "team-analysis-db8-evidence-experiment.json.gz", sha256: options.db8Sha256, contractVersion: "0.7.0" }, sourceDb9: { fileName: "team-analysis-db9-runtime-evidence.json.gz", sha256: options.db9Sha256, contractVersion: "0.8.0" }, sourceDb11: { fileName: "team-analysis-db11-experiment.json.gz", sha256: options.db11Sha256, contractVersion: "0.10.0" }, nativeRuntime: { fileName: "libcocos2dcpp.so", sha256: options.nativeSha256, sizeBytes: options.nativeSizeBytes }, nativeEvidence: { fileName: "native-special-category-semantics.json", sha256: options.evidenceSha256 }, inheritedSemanticPromotionCount: 3, semanticPromotionCount: 1, categoryDictionary: categories, resolutions };
}
export function buildDatabaseTeamAnalysisDb26Coverage(dataset: DatabaseTeamAnalysisDb26Dataset, sourceGapOccurrenceCount: number): DatabaseTeamAnalysisDb26Coverage { const counts: Record<string, number> = {}; for (const value of dataset.resolutions) { const key = value.selector.rawMask === null ? "null" : String(value.selector.rawMask); counts[key] = (counts[key] ?? 0) + 1; } return { schemaVersion: 1, sourceGapOccurrenceCount, resolutionCount: dataset.resolutions.length, affectedStateCount: new Set(dataset.resolutions.map(value => value.stateKey)).size, uniqueCausalityCount: new Set(dataset.resolutions.map(value => value.causalityId)).size, supportedSelectorCount: dataset.resolutions.filter(value => value.selector.status === "supported").length, unknownSelectorCount: dataset.resolutions.filter(value => value.selector.status === "unknown").length, partialActivationCount: dataset.resolutions.length, categoryDictionaryCount: dataset.categoryDictionary.length, rawMaskCounts: Object.fromEntries(Object.entries(counts).sort(([a], [b]) => a.localeCompare(b, "en", { numeric: true }))), inheritedSemanticPromotionCount: 3, semanticPromotionCount: 1 }; }
