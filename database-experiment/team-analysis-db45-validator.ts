import { DatabaseExperimentTables } from "./builder";
import { DatabaseTeamAnalysisDb11Dataset } from "./team-analysis-db11-contract";
import { DatabaseTeamAnalysisDb31Dataset } from "./team-analysis-db31-contract";
import { DatabaseTeamAnalysisDb33Dataset } from "./team-analysis-db33-contract";
import { DatabaseTeamAnalysisDb35Dataset } from "./team-analysis-db35-contract";
import { DatabaseTeamAnalysisDb36Dataset } from "./team-analysis-db36-contract";
import { DatabaseTeamAnalysisDb37Dataset } from "./team-analysis-db37-contract";
import { projectDb45Modifier } from "./team-analysis-db45-builder";
import { DatabaseTeamAnalysisDb45Dataset, Db45AbsorbDamageProjection, Db45NativeEvidence } from "./team-analysis-db45-contract";

export interface Db45Validation { schemaVersion: 1; valid: boolean; timingRuleCount: number; absorbRuleCount: number; losslessReconstructionCount: number; failures: string[] }
export interface Db45ValidationContext { sourceHashes: Record<string, string>; nativeSha256: string; nativeSizeBytes: number; evidence: Db45NativeEvidence; evidenceSha256: string }
const EXPECTED_HASHES = { db8: "814502fd8dfe6f692ea133f60a3c0e4de422de09a5f98b4567a021bf4dfa3658", db9: "99f5b296fc8c8c0fd678c716303e8a0cc02c95092dedc705a0689737d57ee87d", db11: "42ed0aa644dd0d08fa7ca1660b82652327b1fe07e8f710dd8327d16617f7bee3", db31: "1d4dcfeb91df820aea48d34a5496617e197a5c320e53e9efc0a47d806eeff7e1", db33: "70fbd3a714ffeec21392ada5fd070e2c52986ab20d67418d397145071f809024", db35: "513c6c8242b7bcc613ba5b897b346f6900f7ca7372346427cad38cc90e0d97a6", db36: "f94c117e69464b83cf4ba4cb00578bcd4e52539b9116cf6158907f39a1d3bfcb", db37: "ed31b70cc8d0f38dab6bcb9451e1ab2ca05814b51a0e6e5ec05414a4fb4b3a6a" };
const EXPECTED_SOURCES = {
    db8: { fileName: "team-analysis-db8-evidence-experiment.json.gz", contractVersion: "0.7.0" },
    db9: { fileName: "team-analysis-db9-runtime-evidence.json.gz", contractVersion: "0.8.0" },
    db11: { fileName: "team-analysis-db11-experiment.json.gz", contractVersion: "0.10.0" },
    db31: { fileName: "team-analysis-db31-skill-calc-option.json.gz", contractVersion: "0.30.0" },
    db33: { fileName: "team-analysis-db33-player-attack-setup-timing.json.gz", contractVersion: "0.32.0" },
    db35: { fileName: "team-analysis-db35-target-dispatch.json.gz", contractVersion: "0.34.0" },
    db36: { fileName: "team-analysis-db36-sub-target-semantics.json.gz", contractVersion: "0.35.0" },
    db37: { fileName: "team-analysis-db37-passive-lifecycle.json.gz", contractVersion: "0.36.0" },
};
const DATABASE_COLUMNS = ["efficacy_type", "eff_value1", "eff_value2", "eff_value3", "calc_option", "exec_timing_type", "probability", "target_type", "sub_target_type_set_id", "turn", "is_once"];
const ARITHMETIC = { status: "supported", sourceDamage: "displayed_and_mission_counted_player_attack_damage", formula: "truncate_toward_zero((float64(float32_sum) / 100.0) * signed_int32_damage)", controllerArgument: "negated_truncated_result", hpFactorRawType: 2, hpFactorValue: "truncated_result", finalHpApplication: "unknown" };
const id = (value: unknown) => value == null ? undefined : String(value);
const integer = (value: unknown): number | undefined => { const parsed = typeof value === "number" ? value : typeof value === "string" && value.trim() ? Number(value) : NaN; return Number.isSafeInteger(parsed) ? parsed : undefined; };
const key = (value: { stateKey: string; ruleKey: string }) => `${value.stateKey}|${value.ruleKey}`;
const index = <T extends { stateKey: string; ruleKey: string }>(values: T[]) => new Map(values.map(value => [key(value), value]));
export function hasDb45ConservativeBoundaries(value: Pick<Db45AbsorbDamageProjection, "calculationOperation" | "arithmetic" | "registrationGate" | "lifecycle" | "independentDimensions">): boolean {
    return value.calculationOperation.status === "preserved_not_applied" && value.calculationOperation.boundary === "handler_aggregate_and_damage_consumer_do_not_read_calc_option" && value.arithmetic.finalHpApplication === "unknown" && value.registrationGate.status === "partial" && value.registrationGate.semanticName === "unknown" && value.lifecycle.recurrence === "partial" && value.lifecycle.resetAndExpiry === "unknown" && JSON.stringify(value.independentDimensions) === JSON.stringify({ condition: "independent", operation: "preserved_not_applied", target: "inherited_db35_db36_and_runtime_filter", timing: "independent", bucket: "supported_consumer_path", duration: "inherited_db37", recurrence: "partial", expiry: "unknown", probability: "raw_unknown_order" });
}
export function validateDatabaseTeamAnalysisDb45Dataset(dataset: DatabaseTeamAnalysisDb45Dataset, db11: DatabaseTeamAnalysisDb11Dataset, db31: DatabaseTeamAnalysisDb31Dataset, db33: DatabaseTeamAnalysisDb33Dataset, db35: DatabaseTeamAnalysisDb35Dataset, db36: DatabaseTeamAnalysisDb36Dataset, db37: DatabaseTeamAnalysisDb37Dataset, tables: DatabaseExperimentTables, context: Db45ValidationContext): Db45Validation {
    const failures: string[] = [];
    if (dataset.schemaVersion !== 1 || dataset.contract !== "dokkan-team-analysis-player-attack-post-damage-and-hp-factor-native-semantics-experiment" || dataset.contractVersion !== "0.44.0" || dataset.generatedAt !== db37.generatedAt || dataset.sourceSnapshotVersion !== db37.sourceSnapshotVersion || dataset.sourceDatabaseSha256 !== db37.sourceDatabaseSha256 || dataset.inheritedSemanticPromotionCount !== 53 || dataset.semanticPromotionCount !== 5) failures.push("contract identity mismatch");
    if (JSON.stringify(context.sourceHashes) !== JSON.stringify(EXPECTED_HASHES) || dataset.sourceDatabaseSha256 !== "3654eb7db9e18dfe4c238abd02bcc06a688ffa6f30aa1ad93fd108dcfeb78265" || JSON.stringify(dataset.nativeRuntime) !== JSON.stringify({ fileName: "libcocos2dcpp.so", sha256: context.nativeSha256, sizeBytes: context.nativeSizeBytes }) || JSON.stringify(dataset.nativeEvidence) !== JSON.stringify({ fileName: "native-player-attack-post-damage-hp-factor-semantics.json", sha256: context.evidenceSha256 }) || context.evidence.sourceSha256 !== context.nativeSha256) failures.push("source lineage mismatch");
    for (const [name, hash] of Object.entries(EXPECTED_HASHES)) { const source = EXPECTED_SOURCES[name as keyof typeof EXPECTED_SOURCES]; if (JSON.stringify(dataset.sources[name]) !== JSON.stringify({ fileName: source.fileName, sha256: hash, contractVersion: source.contractVersion })) failures.push(`dataset source mismatch ${name}`); }
    if (dataset.timingRuleProjections.length !== db33.ruleTimings.length || new Set(dataset.timingRuleProjections.map(key)).size !== dataset.timingRuleProjections.length) failures.push("timing projection cardinality mismatch");
    const sourceTimings = index(db33.ruleTimings), projectedTimings = index(dataset.timingRuleProjections);
    for (const timing of dataset.timingRuleProjections) { const source = sourceTimings.get(key(timing)); if (!source || timing.passiveSkillId !== source.passiveSkillId || timing.rawExecutionTimingType !== source.rawExecutionTimingType || timing.effectCount !== source.effectCount || JSON.stringify(timing.calculationOperation) !== JSON.stringify(source.calculationOperation) || JSON.stringify(timing.independentDimensions) !== JSON.stringify(source.independentDimensions)) { failures.push(`timing reconstruction mismatch ${key(timing)}`); continue; } const raw = integer(timing.rawExecutionTimingType); if (raw === 5) { const expectedRuntime = { fileName: "libcocos2dcpp.so", sha256: context.nativeSha256, evidenceFile: "native-player-attack-post-damage-hp-factor-semantics.json", evidenceSha256: context.evidenceSha256, proofRoles: ["execution_filter", "player_attack_setup"] }; if (JSON.stringify(timing.executionTiming) !== JSON.stringify({ status: "supported", event: "player_attack_post_damage_setup", sequence: "after_damage_calculation_enemy_after_hp_update_and_attack_recording_before_enemy_round_timing_5_and_setup_return" }) || JSON.stringify(timing.provenance) !== JSON.stringify({ database: source.provenance.database, runtime: expectedRuntime })) failures.push(`timing-5 promotion mismatch ${key(timing)}`); } else if (JSON.stringify(timing.executionTiming) !== JSON.stringify(source.executionTiming) || JSON.stringify(timing.provenance) !== JSON.stringify(source.provenance)) failures.push(`non-timing-5 mutation ${key(timing)}`); }
    const operations = index(db31.ruleProjections), targets = index(db35.ruleTargets), subTargets = index(db36.ruleSubTargets), lifecycles = index(db37.ruleLifecycles), rows = new Map((tables.passive_skills ?? []).map(row => [id(row.id)!, row])), legacy = new Map<string, string | null>(); for (const state of db11.states) for (const rule of state.passive?.rules ?? []) legacy.set(`${state.stateKey}|${rule.ruleKey}`, rule.effects[0]?.kind ?? null);
    const expectedIds = db37.ruleLifecycles.filter(value => integer(rows.get(value.passiveSkillId)?.efficacy_type) === 28); if (dataset.absorbDamageProjections.length !== expectedIds.length || new Set(dataset.absorbDamageProjections.map(key)).size !== dataset.absorbDamageProjections.length) failures.push("absorb projection cardinality mismatch");
    let reconstructed = 0;
    for (const value of dataset.absorbDamageProjections) {
        const row = rows.get(value.passiveSkillId), operation = operations.get(key(value)), timing = projectedTimings.get(key(value)), target = targets.get(key(value)), subTarget = subTargets.get(key(value)), lifecycle = lifecycles.get(key(value)); if (!row || integer(row.efficacy_type) !== 28 || !operation || !timing || !target || !subTarget || !lifecycle) { failures.push(`absorb source missing ${key(value)}`); continue; }
        const rawFields = [value.modifier.rawPercentagePoints, value.ignoredNumericParameters.rawEffValue2, value.ignoredNumericParameters.rawEffValue3, value.calculationOperation.rawCalcOption, value.rawProbability, value.target.subTarget.rawSetId, value.lifecycle.duration.rawTurn, value.lifecycle.onceOnly.rawIsOnce], sourceFields = [row.eff_value1, row.eff_value2, row.eff_value3, row.calc_option, row.probability, row.sub_target_type_set_id, row.turn, row.is_once];
        const expectedModifier = projectDb45Modifier(row.eff_value1), expectedSimulation = expectedModifier.status === "supported" && timing.executionTiming.status === "supported" ? "partial" : "unknown";
        if (value.efficacyType !== 28 || value.sourceEffectCount !== lifecycle.effectCount || JSON.stringify(rawFields) !== JSON.stringify(sourceFields) || JSON.stringify(value.modifier) !== JSON.stringify(expectedModifier) || JSON.stringify(value.ignoredNumericParameters) !== JSON.stringify({ status: "not_read_by_handler_or_consumer", rawEffValue2: row.eff_value2, rawEffValue3: row.eff_value3 }) || JSON.stringify(value.calculationOperation.db31Operation) !== JSON.stringify(operation.operation) || JSON.stringify(value.executionTiming) !== JSON.stringify(timing.executionTiming) || JSON.stringify(value.target.candidate) !== JSON.stringify(target.target.value) || JSON.stringify(value.target.subTarget) !== JSON.stringify({ status: "supported", rawSetId: subTarget.rawSetId, composition: subTarget.composition, emptySetBehavior: subTarget.emptySetBehavior, filters: subTarget.filters }) || JSON.stringify(value.lifecycle.duration) !== JSON.stringify(lifecycle.duration) || JSON.stringify(value.lifecycle.onceOnly) !== JSON.stringify(lifecycle.onceOnly) || value.simulationStatus !== expectedSimulation || !hasDb45ConservativeBoundaries(value)) failures.push(`absorb reconstruction mismatch ${key(value)}`); else reconstructed++;
        if (value.target.status !== "supported" || value.target.candidate.scope !== "self" || value.target.aggregateFilter !== "matching_deck_index_and_skill_category" || value.target.subTarget.filters.length !== 0 || JSON.stringify(value.calculationBucket) !== JSON.stringify({ status: "supported", value: "action_bank_player_damage_to_hp_factor" }) || JSON.stringify(value.stacking) !== JSON.stringify({ status: "supported", value: "float32_add_in_stored_record_order_for_matching_deck_and_category" }) || JSON.stringify(value.arithmetic) !== JSON.stringify(ARITHMETIC) || JSON.stringify(value.registrationGate) !== JSON.stringify({ status: "partial", callChangeParamOffset: 4, requiredValue: 0, semanticName: "unknown" })) failures.push(`absorb semantic boundary mismatch ${key(value)}`);
        const runtime = { fileName: "libcocos2dcpp.so", sha256: context.nativeSha256, evidenceFile: "native-player-attack-post-damage-hp-factor-semantics.json", evidenceSha256: context.evidenceSha256, proofRoles: context.evidence.codeRegions.map(region => region.role) }, inherited = { db8Sha256: context.sourceHashes.db8, db9Sha256: context.sourceHashes.db9, db11Sha256: context.sourceHashes.db11, db31Sha256: context.sourceHashes.db31, db33Sha256: context.sourceHashes.db33, db35Sha256: context.sourceHashes.db35, db36Sha256: context.sourceHashes.db36, db37Sha256: context.sourceHashes.db37 };
        if (JSON.stringify(value.provenance) !== JSON.stringify({ database: { table: "passive_skills", rowId: value.passiveSkillId, columns: DATABASE_COLUMNS }, runtime, inherited })) failures.push(`absorb provenance mismatch ${key(value)}`);
        const currentKind = legacy.get(key(value)) ?? null; if (value.legacyComparison.currentKind !== currentKind || value.legacyComparison.status !== (currentKind === "unknown" ? "representation_gain_legacy_unknown" : "not_comparable") || value.legacyComparison.confirmedConflict !== false) failures.push(`legacy comparison mismatch ${key(value)}`);
    }
    return { schemaVersion: 1, valid: failures.length === 0, timingRuleCount: dataset.timingRuleProjections.length, absorbRuleCount: dataset.absorbDamageProjections.length, losslessReconstructionCount: reconstructed, failures };
}
