import { DatabaseExperimentTables } from "./builder";
import { SqliteScalar } from "./contract";
import { DatabaseTeamAnalysisDb11Dataset } from "./team-analysis-db11-contract";
import { DatabaseTeamAnalysisDb33Dataset } from "./team-analysis-db33-contract";
import { DatabaseTeamAnalysisDb35Dataset } from "./team-analysis-db35-contract";
import { DatabaseTeamAnalysisDb36Dataset } from "./team-analysis-db36-contract";
import { DatabaseTeamAnalysisDb37Dataset } from "./team-analysis-db37-contract";
import { DatabaseTeamAnalysisDb43Dataset, Db43NativeEvidence } from "./team-analysis-db43-contract";

export interface Db43Validation { schemaVersion: 1; valid: boolean; resolutionCount: number; losslessReconstructionCount: number; failures: string[] }
const integer = (value: unknown): number | undefined => { const parsed = typeof value === "number" ? value : typeof value === "string" && value.trim() ? Number(value) : NaN; return Number.isSafeInteger(parsed) ? parsed : undefined; };
const key = (value: { stateKey: string; ruleKey: string }) => `${value.stateKey}|${value.ruleKey}`;
const index = <T extends { stateKey: string; ruleKey: string }>(values: T[]) => new Map(values.map(value => [key(value), value]));
const FORMULA = "defender_has_efficacy_78 || (!defender_has_efficacy_24 && !independent_raw_attacker_override && !independent_player_mode_override && element_affinity_result_eq_1)";
const INPUTS = ["attacker_element_type", "defender_element_type", "defender_has_efficacy_24", "independent_raw_attacker_override", "defender_has_efficacy_78", "independent_player_mode_override"];
const DATABASE_COLUMNS = ["efficacy_type", "exec_timing_type", "exec_game_type", "target_type", "sub_target_type_set_id", "calc_option", "turn", "is_once", "probability", "causality_conditions", "eff_value1", "eff_value2", "eff_value3"];
export interface Db43ValidationContext { db8Sha256: string; db9Sha256: string; db11Sha256: string; db33Sha256: string; db35Sha256: string; db36Sha256: string; db37Sha256: string; currentTeamAnalysisSha256: string; nativeSha256: string; nativeSizeBytes: number; evidence: Db43NativeEvidence; evidenceSha256: string }

export function validateDatabaseTeamAnalysisDb43Dataset(dataset: DatabaseTeamAnalysisDb43Dataset, db11: DatabaseTeamAnalysisDb11Dataset, db33: DatabaseTeamAnalysisDb33Dataset, db35: DatabaseTeamAnalysisDb35Dataset, db36: DatabaseTeamAnalysisDb36Dataset, db37: DatabaseTeamAnalysisDb37Dataset, tables: DatabaseExperimentTables, context: Db43ValidationContext): Db43Validation {
    const failures: string[] = [], source = new Map<string, { passiveSkillId: string; conditionStatus: string }>();
    if (dataset.schemaVersion !== 1 || dataset.contract !== "dokkan-team-analysis-guard-disable-native-semantics-experiment" || dataset.contractVersion !== "0.42.0" || dataset.generatedAt !== db11.generatedAt || dataset.sourceSnapshotVersion !== db11.sourceSnapshotVersion || dataset.sourceDatabaseSha256 !== db11.sourceSha256 || dataset.inheritedSemanticPromotionCount !== 50 || dataset.semanticPromotionCount !== 1) failures.push("dataset identity");
    const expectedLineage = { sourceDb8: { fileName: "team-analysis-db8-evidence-experiment.json.gz", sha256: context.db8Sha256, contractVersion: "0.7.0" }, sourceDb9: { fileName: "team-analysis-db9-runtime-evidence.json.gz", sha256: context.db9Sha256, contractVersion: "0.8.0" }, sourceDb11: { fileName: "team-analysis-db11-experiment.json.gz", sha256: context.db11Sha256, contractVersion: "0.10.0" }, sourceDb33: { fileName: "team-analysis-db33-player-attack-setup-timing.json.gz", sha256: context.db33Sha256, contractVersion: "0.32.0" }, sourceDb35: { fileName: "team-analysis-db35-target-dispatch.json.gz", sha256: context.db35Sha256, contractVersion: "0.34.0" }, sourceDb36: { fileName: "team-analysis-db36-sub-target-semantics.json.gz", sha256: context.db36Sha256, contractVersion: "0.35.0" }, sourceDb37: { fileName: "team-analysis-db37-passive-lifecycle.json.gz", sha256: context.db37Sha256, contractVersion: "0.36.0" }, currentTeamAnalysis: { fileName: "team-analysis.json.gz", sha256: context.currentTeamAnalysisSha256 }, nativeRuntime: { fileName: "libcocos2dcpp.so", sha256: context.nativeSha256, sizeBytes: context.nativeSizeBytes }, nativeEvidence: { fileName: "native-guard-disable-semantics.json", sha256: context.evidenceSha256 } };
    if (JSON.stringify({ sourceDb8: dataset.sourceDb8, sourceDb9: dataset.sourceDb9, sourceDb11: dataset.sourceDb11, sourceDb33: dataset.sourceDb33, sourceDb35: dataset.sourceDb35, sourceDb36: dataset.sourceDb36, sourceDb37: dataset.sourceDb37, currentTeamAnalysis: dataset.currentTeamAnalysis, nativeRuntime: dataset.nativeRuntime, nativeEvidence: dataset.nativeEvidence }) !== JSON.stringify(expectedLineage)) failures.push("dataset lineage");
    for (const state of db11.states) for (const rule of state.passive?.rules ?? []) if (integer(rule.source.efficacyType) === 24) source.set(key({ stateKey: state.stateKey, ruleKey: rule.ruleKey }), { passiveSkillId: rule.source.passiveSkillId, conditionStatus: rule.conditionStatus });
    const rows = new Map<string, Record<string, SqliteScalar>>((tables.passive_skills ?? []).map(row => [String(row.id), row])), timings = index(db33.ruleTimings), targets = index(db35.ruleTargets), subTargets = index(db36.ruleSubTargets), lifecycles = index(db37.ruleLifecycles), seen = new Set<string>();
    let losslessReconstructionCount = 0;
    for (const value of dataset.resolutions) {
        const k = key(value), expected = source.get(k), row = rows.get(value.passiveSkillId), timing = timings.get(k), target = targets.get(k), subTarget = subTargets.get(k), lifecycle = lifecycles.get(k);
        if (seen.has(k)) failures.push(`duplicate ${k}`); seen.add(k);
        if (!expected || expected.passiveSkillId !== value.passiveSkillId || expected.conditionStatus !== value.conditionStatus) failures.push(`source identity ${k}`);
        if (!row || integer(row.efficacy_type) !== 24 || JSON.stringify([row.eff_value1, row.eff_value2, row.eff_value3, row.calc_option, row.target_type, row.sub_target_type_set_id, row.exec_timing_type, row.exec_game_type, row.probability, row.causality_conditions]) !== JSON.stringify([value.effect.behavioralParameters.rawEffValue1, value.effect.behavioralParameters.rawEffValue2, value.effect.behavioralParameters.rawEffValue3, value.effect.rawCalculationOption, value.target.rawTargetType, value.target.subTarget.rawSetId, value.executionTiming.raw, value.raw.execGameType, value.raw.probability, value.raw.causalityConditions])) failures.push(`lossless sqlite reconstruction ${k}`); else losslessReconstructionCount += 1;
        if (!timing || value.effect.declaredCalculationOperation !== timing.calculationOperation.value || JSON.stringify(value.executionTiming.value) !== JSON.stringify(timing.executionTiming)) failures.push(`timing ${k}`);
        if (!target || target.target.status !== "supported" || JSON.stringify(value.target.candidate) !== JSON.stringify(target.target.value)) failures.push(`target ${k}`);
        if (!subTarget || subTarget.status !== "supported" || subTarget.filters.length !== 0 || value.target.subTarget.filters.length !== 0 || value.target.subTarget.emptySetBehavior !== "identity") failures.push(`sub-target ${k}`);
        if (!lifecycle || JSON.stringify(value.lifecycle.duration) !== JSON.stringify(lifecycle.duration) || JSON.stringify(value.lifecycle.onceOnly) !== JSON.stringify(lifecycle.onceOnly)) failures.push(`lifecycle ${k}`);
        if (value.effect.kind !== "disable_normal_element_affinity_guard" || value.effect.unit !== "boolean_presence" || value.effect.nativeFormula !== FORMULA || JSON.stringify(value.effect.nativeInputs) !== JSON.stringify(INPUTS) || value.effect.normalAffinityGuardWhenPresent !== "disabled_unless_independent_efficacy_78_override" || value.effect.behavioralParameters.status !== "not_read_by_guard_decision" || value.effect.calculationOperationApplication !== "not_applied_by_no_value_handler_or_guard_presence_consumer" || value.calculation.consumerPhase !== "guard_resolution_before_element_coefficient" || value.calculation.calculationBucket !== "unknown" || value.calculation.stackingInGuardDecision !== "boolean_presence" || value.lifecycle.recurrence !== "partial" || value.lifecycle.resetAndExpiry !== "unknown") failures.push(`semantic boundary ${k}`);
        const expectedRuntime = { fileName: "libcocos2dcpp.so", sha256: context.nativeSha256, evidenceFile: "native-guard-disable-semantics.json", evidenceSha256: context.evidenceSha256, codeRegions: context.evidence.codeRegions }, expectedInherited = { db8Sha256: context.db8Sha256, db9Sha256: context.db9Sha256, db11Sha256: context.db11Sha256, db33Sha256: context.db33Sha256, db35Sha256: context.db35Sha256, db36Sha256: context.db36Sha256, db37Sha256: context.db37Sha256, currentTeamAnalysisSha256: context.currentTeamAnalysisSha256 };
        if (value.provenance.database.table !== "passive_skills" || value.provenance.database.rowId !== value.passiveSkillId || JSON.stringify(value.provenance.database.columns) !== JSON.stringify(DATABASE_COLUMNS) || JSON.stringify(value.provenance.runtime) !== JSON.stringify(expectedRuntime) || JSON.stringify(value.provenance.inherited) !== JSON.stringify(expectedInherited)) failures.push(`provenance ${k}`);
    }
    if (source.size !== 34 || dataset.resolutions.length !== source.size || seen.size !== source.size || [...source.keys()].some(value => !seen.has(value))) failures.push(`cardinality ${dataset.resolutions.length}/${source.size}`);
    return { schemaVersion: 1, valid: failures.length === 0, resolutionCount: dataset.resolutions.length, losslessReconstructionCount, failures };
}
