import { DatabaseTeamAnalysisDb15Coverage, DatabaseTeamAnalysisDb15Dataset } from "./team-analysis-db15-contract";

export function renderDatabaseTeamAnalysisDb15Report(dataset: DatabaseTeamAnalysisDb15Dataset, coverage: DatabaseTeamAnalysisDb15Coverage): string {
    const improved = dataset.ruleConditionParity.filter(value => value.baseline.status !== value.withCompatibilityAliases.status || value.baseline.databaseOnlySignatures.length !== value.withCompatibilityAliases.databaseOnlySignatures.length || value.baseline.currentOnlySignatures.length !== value.withCompatibilityAliases.currentOnlySignatures.length);
    const examples = improved.map(value => `- \`${value.stateKey}\`, DB \`${value.databaseRuleKey}\` → current \`${value.currentRuleKey}\`: ${value.baseline.status} (${value.baseline.databaseOnlySignatures.length} DB-only/${value.baseline.currentOnlySignatures.length} current-only) → ${value.withCompatibilityAliases.status} (${value.withCompatibilityAliases.databaseOnlySignatures.length}/${value.withCompatibilityAliases.currentOnlySignatures.length}); ${value.appliedCompatibilityAliases.length} alias(es).`).join("\n");
    return `# Database Team Analysis experiment — DB15 rule-condition parity\n\nDB15 measures the supported runtime-condition subset on DB13-aligned rule pairs before and after applying DB14 aliases as comparison-only metadata. Native DB11 conditions are never rewritten.\n\n## Coverage\n\n- Aligned rule pairs: **${coverage.alignedRulePairCount}**; comparable: **${coverage.comparableRulePairCount}**; without supported comparable conditions: **${coverage.noComparableConditionRulePairCount}**.\n- Baseline exact/partial/divergent: **${coverage.baselinePairCounts.exact}/${coverage.baselinePairCounts.partial}/${coverage.baselinePairCounts.divergent}**.\n- With compatibility aliases exact/partial/divergent: **${coverage.compatibilityPairCounts.exact}/${coverage.compatibilityPairCounts.partial}/${coverage.compatibilityPairCounts.divergent}**; exact delta: **${coverage.exactPairDelta >= 0 ? "+" : ""}${coverage.exactPairDelta}**.\n- Matched signature occurrences: **${coverage.baselineMatchedSignatureOccurrenceCount} → ${coverage.compatibilityMatchedSignatureOccurrenceCount}**.\n- DB-only signature occurrences: **${coverage.baselineDatabaseOnlySignatureOccurrenceCount} → ${coverage.compatibilityDatabaseOnlySignatureOccurrenceCount}**.\n- Current-only signature occurrences: **${coverage.baselineCurrentOnlySignatureOccurrenceCount} → ${coverage.compatibilityCurrentOnlySignatureOccurrenceCount}**.\n- Applied aliases/native signatures resolved/current exact signatures matched: **${coverage.appliedCompatibilityAliasCount}/${coverage.resolvedNativeSignatureOccurrenceCount}/${coverage.matchedCurrentExactSignatureOccurrenceCount}**.\n- Semantic promotions: **0**.\n\n## Changed aligned pairs\n\n${examples || "None."}\n\n## Boundary\n\nThe comparison universe is \`${dataset.comparisonUniverse}\`. Counts are rule-pair occurrences, so a structural signature repeated across aligned pairs remains visible in each pair. DB14 normalization replaces two proven native bounds with one current-compatible exact-turn signature only inside the parity view; the source records remain authoritative.\n\n## Gate assessment\n\nThe aliases produce a measured parity improvement, but remaining divergences and conditions outside the supported universe keep production replacement at NO-GO. The next gate should isolate the highest-impact residual pattern without inferring a domain boundary that first-party evidence has not proven.\n`;
}
