import { DatabaseTeamAnalysisDb27Coverage } from "./team-analysis-db27-contract";
export function renderDatabaseTeamAnalysisDb27Report(coverage: DatabaseTeamAnalysisDb27Coverage): string { return `# Database Team Analysis experiment — DB27 target HP predicates\n\nDB27 promotes native parameter, metric and comparator semantics for causality types 17, 18 and 33.\n\n- Resolutions/states: **${coverage.resolutionCount}/${coverage.affectedStateCount}**.\n- Type 17/18/33 occurrences: **${coverage.occurrenceCountsByType["17"]}/${coverage.occurrenceCountsByType["18"]}/${coverage.occurrenceCountsByType["33"]}**.\n- Partial resolutions: **${coverage.partialResolutionCount}**.\n- Semantic promotions: **3**.\n\nTypes 17 and 18 calculate selected-enemy current HP divided by max HP times 100 as a floating-point value and compare \`cau_val1\` inclusively with \`>=\` or \`<=\`. They require an unnamed runtime selection flag to equal zero; its semantic name and zero-max policy remain unknown.\n\nType 33 reads \`cau_val1/2\` as an inclusive interval. The same runtime flag structurally selects player HP when zero and enemy HP when nonzero. Enemy HP uses the pinned \`InGameHpRate::calcHpRate\` helper: zero numerator or denominator returns zero; ratios strictly between 0 and 1 normalize to 1, ratios strictly between 99 and 100 normalize to 99, then conversion rounds to nearest with ties away from zero. No generic maximum clamp is claimed.\n\nTiming, recurrence and calculation bucket remain unknown. All records are therefore partial even though metric, parameter and comparator semantics are supported.\n`; }
