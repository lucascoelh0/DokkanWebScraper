import{equal,throws}from"assert";import{describe,it}from"mocha";import{parseDb32ExecutionTiming,projectDb32ExecutionTiming,validateDb32SourceLineage}from"./team-analysis-db32-builder";
describe("DB32 execution timing",()=>{it("promotes only raw value 1",()=>{equal(projectDb32ExecutionTiming(1).status,"supported");for(const v of[0,3,4,5,6,7,9,11,12,14,15,-1,null," "])equal(projectDb32ExecutionTiming(v).status,"unknown")});it("rejects unsafe and fractional enum values",()=>{equal(Number.isSafeInteger(parseDb32ExecutionTiming(Number.MAX_SAFE_INTEGER+1)),false);equal(projectDb32ExecutionTiming(1.5).status,"unknown")});it("keeps timing separate from calculation phases",()=>equal((projectDb32ExecutionTiming(1)as any).calculationBucket,undefined));it("rejects DB31 lineage from another database or runtime",()=>{const db11={contractVersion:"0.10.0",sourceSnapshotVersion:"snapshot",sourceSha256:"db"}as any,db31={contractVersion:"0.30.0",sourceSnapshotVersion:"snapshot",sourceDatabaseSha256:"db",sourceDb11:{sha256:"db11"},nativeRuntime:{sha256:"elf",sizeBytes:10}}as any;validateDb32SourceLineage(db11,db31,"db11","elf",10);throws(()=>validateDb32SourceLineage(db11,{...db31,sourceDatabaseSha256:"other"},"db11","elf",10),/lineage/);throws(()=>validateDb32SourceLineage(db11,{...db31,nativeRuntime:{sha256:"other",sizeBytes:10}},"db11","elf",10),/lineage/);throws(()=>validateDb32SourceLineage(db11,{...db31,nativeRuntime:{sha256:"elf",sizeBytes:11}},"db11","elf",10),/lineage/)})});
