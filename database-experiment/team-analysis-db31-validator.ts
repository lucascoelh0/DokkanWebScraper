import{parseDb31CalculationOption}from"./team-analysis-db31-builder";import{DatabaseTeamAnalysisDb11Dataset}from"./team-analysis-db11-contract";import{DatabaseTeamAnalysisDb31Dataset}from"./team-analysis-db31-contract";
export interface Db31Validation{schemaVersion:1,valid:boolean,ruleCount:number,losslessReconstructionCount:number,failures:string[]}
export function validateDatabaseTeamAnalysisDb31Dataset(d:DatabaseTeamAnalysisDb31Dataset,source:DatabaseTeamAnalysisDb11Dataset):Db31Validation{const failures:string[]=[],expected=new Map<string,{passiveSkillId:string,raw:unknown,effectCount:number}>();for(const s of source.states)for(const r of s.passive?.rules??[]){const k=`${s.stateKey}|${r.ruleKey}`;if(expected.has(k))failures.push(`duplicate source ${k}`);expected.set(k,{passiveSkillId:r.source.passiveSkillId,raw:r.source.calculationOption,effectCount:r.effects.length});}const seen=new Set<string>();let lossless=0;for(const r of d.ruleProjections){const k=`${r.stateKey}|${r.ruleKey}`,x=expected.get(k);if(seen.has(k))failures.push(`duplicate projection ${k}`);seen.add(k);if(!x){failures.push(`unexpected projection ${k}`);continue;}if(r.passiveSkillId!==x.passiveSkillId||JSON.stringify(r.rawCalculationOption)!==JSON.stringify(x.raw)||r.effectCount!==x.effectCount)failures.push(`lossless mismatch ${k}`);else lossless++;if(r.provenance.database.table!=="passive_skills"||r.provenance.database.rowId!==r.passiveSkillId||r.provenance.database.column!=="calc_option")failures.push(`database provenance mismatch ${k}`);const n=parseDb31CalculationOption(r.rawCalculationOption);const known=Number.isSafeInteger(n)&&n>=0&&n<=4;if((r.operation.status==="supported")!==known)failures.push(`support boundary mismatch ${k}`);if(Object.values(r.independentDimensions).some(v=>v!=="unknown"))failures.push(`independent dimension promoted ${k}`);}if(seen.size!==expected.size)failures.push(`cardinality ${seen.size}/${expected.size}`);if(d.enumValues.map(v=>v.raw).join(",")!=="0,1,2,3,4")failures.push("enum domain/order mismatch");return{schemaVersion:1,valid:failures.length===0,ruleCount:d.ruleProjections.length,losslessReconstructionCount:lossless,failures};}
