import { DatabaseTeamAnalysisDb18Coverage, DatabaseTeamAnalysisDb18Dataset } from "./team-analysis-db18-contract";
export function renderDatabaseTeamAnalysisDb18Report(dataset: DatabaseTeamAnalysisDb18Dataset, coverage: DatabaseTeamAnalysisDb18Coverage): string { const examples = [...dataset.affectedRuleParity].sort((left, right) => Number(right.beforeLifecycleCompatibility.status !== right.withLifecycleCompatibility.status) - Number(left.beforeLifecycleCompatibility.status !== left.withLifecycleCompatibility.status) || (right.beforeLifecycleCompatibility.databaseOnlySignatures.length + right.beforeLifecycleCompatibility.currentOnlySignatures.length - right.withLifecycleCompatibility.databaseOnlySignatures.length - right.withLifecycleCompatibility.currentOnlySignatures.length) - (left.beforeLifecycleCompatibility.databaseOnlySignatures.length + left.beforeLifecycleCompatibility.currentOnlySignatures.length - left.withLifecycleCompatibility.databaseOnlySignatures.length - left.withLifecycleCompatibility.currentOnlySignatures.length) || left.stateKey.localeCompare(right.stateKey, "en", { numeric: true })).slice(0, 12).map(value => `- \`${value.stateKey}\`, DB \`${value.databaseRuleKey}\` → current \`${value.currentRuleKey}\`: ${value.beforeLifecycleCompatibility.status} → ${value.withLifecycleCompatibility.status}; ${value.applications.map(item => item.kind).join(", ")}.`).join("\n"); return `# Database Team Analysis experiment — DB18 lifecycle compatibility parity\n\nDB18 applies DB17 conclusions only to the DB16-attributed occurrences and only under \`${dataset.compatibilityPrecondition}\`. DB11 and DB15 remain unchanged.\n\n- Comparable/affected rule pairs: **${coverage.comparableRulePairCount}/${coverage.affectedRulePairCount}**.\n- Exact/partial/divergent before: **${coverage.beforePairCounts.exact}/${coverage.beforePairCounts.partial}/${coverage.beforePairCounts.divergent}**.\n- Exact/partial/divergent after: **${coverage.lifecycleCompatibilityPairCounts.exact}/${coverage.lifecycleCompatibilityPairCounts.partial}/${coverage.lifecycleCompatibilityPairCounts.divergent}**; exact delta **+${coverage.exactPairDelta}**.\n- Removed current \`gte 1\` tautologies: **${coverage.removedCurrentTautologyOccurrenceCount}**.\n- Normalized DB \`lte 1\` → current \`eq 1\`: **${coverage.normalizedTurnOneBoundaryOccurrenceCount}**.\n- Resolved DB16 attributions: **${coverage.resolvedDb16AttributionCount}**.\n- DB-only residual occurrences: **${coverage.beforeDatabaseOnlySignatureOccurrenceCount} → ${coverage.lifecycleDatabaseOnlySignatureOccurrenceCount}**.\n- Current-only residual occurrences: **${coverage.beforeCurrentOnlySignatureOccurrenceCount} → ${coverage.lifecycleCurrentOnlySignatureOccurrenceCount}**.\n- Remaining residual rule pairs: **${coverage.remainingResidualRulePairCount}**.\n- Inherited/new semantic promotions: **3/0**.\n\n## Highest-impact examples\n\n${examples}\n\nThis is compatibility metadata, not a rewrite of native truth conditions. Production replacement remains NO-GO while other residuals and unsupported condition families remain.\n`; }
