import{equal,throws}from"assert";import{existsSync,readFileSync}from"fs";import{resolve}from"path";import{calculateDb40Metric,evaluateDb40Threshold,validateDb40NativeEvidence}from"./team-analysis-db40-builder";
const p=existsSync(resolve(__dirname,"native-battle-gauge-threshold-semantics.json"))?resolve(__dirname,"native-battle-gauge-threshold-semantics.json"):resolve(__dirname,"..","..","database-experiment","native-battle-gauge-threshold-semantics.json"),e=JSON.parse(readFileSync(p,"utf8"));
describe("database Team Analysis DB40 battle-gauge threshold",()=>{it("uses float32 ratio then truncates toward zero",()=>{equal(calculateDb40Metric(0,3,4),75);equal(calculateDb40Metric(0,1,3),33);equal(calculateDb40Metric(0,-1,3),-33)});it("keeps raw category and zero denominator boundaries",()=>{equal(calculateDb40Metric(1,999,0),0);equal(calculateDb40Metric(0,1,0),null);equal(calculateDb40Metric("bad",1,1),null)});it("distinguishes inclusive over from strict under",()=>{equal(evaluateDb40Threshold(3,100,100),true);equal(evaluateDb40Threshold(4,100,100),false);equal(evaluateDb40Threshold(4,99,100),true);equal(evaluateDb40Threshold(5,1,1),null)});it("rejects substituted evidence",()=>{const x=JSON.parse(JSON.stringify(e));x.metric.formula="substituted";throws(()=>validateDb40NativeEvidence({}as any,x,e.sourceSha256),/identity/);const y=JSON.parse(JSON.stringify(e));y.handlers[1].comparator="lte";throws(()=>validateDb40NativeEvidence({}as any,y,e.sourceSha256),/handler/)})});
