import { existsSync } from "fs"; import { readFile } from "fs/promises"; import { resolve } from "path"; import { DatabaseTeamAnalysisDb27Coverage, DatabaseTeamAnalysisDb27Dataset } from "./team-analysis-db27-contract";
interface Fixture { name: string, stateKey: string, ruleKey: string, causalityId: string, causalityType: 17 | 18 | 33, comparator: string, first: number, second?: number } export interface Db27GoldenValidation { schemaVersion: 1, fixtureCount: number, passed: number, failures: Array<{ fixture: string, issue: string }> }
export async function validateDatabaseTeamAnalysisDb27Goldens(dataset: DatabaseTeamAnalysisDb27Dataset, coverage: DatabaseTeamAnalysisDb27Coverage): Promise<Db27GoldenValidation> { const path = existsSync(resolve(__dirname, "team-analysis-db27-golden-fixtures.json")) ? resolve(__dirname, "team-analysis-db27-golden-fixtures.json") : resolve(__dirname, "..", "..", "database-experiment", "team-analysis-db27-golden-fixtures.json"), fixtures = JSON.parse(await readFile(path, "utf8")) as Fixture[], failures: Array<{ fixture: string, issue: string }> = []; for (const f of fixtures) { const v = dataset.resolutions.find(row => row.stateKey === f.stateKey && row.ruleKey === f.ruleKey && row.causalityId === f.causalityId), issues: string[] = []; if (!v) issues.push("resolution missing"); else { const p = v.predicate; const first = p.metric === "runtime_selected_hp_percent_integer" ? p.lower : p.threshold, second = p.metric === "runtime_selected_hp_percent_integer" ? p.upper : undefined; if (v.causalityType !== f.causalityType || p.comparator !== f.comparator || first !== f.first || second !== f.second) issues.push("predicate differs"); if (v.semanticStatus !== "partial" || v.activation.timing !== "unknown") issues.push("boundary differs"); } if (issues.length) failures.push({ fixture: f.name, issue: issues.join(", ") }); } if (coverage.sourceGapOccurrenceCount !== 59 || coverage.resolutionCount !== 59 || coverage.occurrenceCountsByType["17"] !== 14 || coverage.occurrenceCountsByType["18"] !== 33 || coverage.occurrenceCountsByType["33"] !== 12 || coverage.invalidThresholdCount !== 0) failures.push({ fixture: "coverage", issue: "snapshot accounting differs" }); return { schemaVersion: 1, fixtureCount: fixtures.length + 1, passed: fixtures.length + 1 - failures.length, failures }; }
