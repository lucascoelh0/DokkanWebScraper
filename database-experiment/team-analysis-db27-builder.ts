import { createHash } from "crypto";
import { DatabaseExperimentTables } from "./builder";
import { SqliteScalar } from "./contract";
import { NativeRuntimeElfInspection } from "./native-runtime-elf-adapter";
import { Db11ConditionExpression, DatabaseTeamAnalysisDb11Dataset } from "./team-analysis-db11-contract";
import { DatabaseTeamAnalysisDb8Dataset } from "./team-analysis-db8-contract";
import { DatabaseTeamAnalysisDb9Dataset } from "./team-analysis-db9-contract";
import { DatabaseTeamAnalysisDb27Coverage, DatabaseTeamAnalysisDb27Dataset, Db27NativeTargetHpEvidence, Db27TargetHpPredicate, Db27TargetHpResolution } from "./team-analysis-db27-contract";
const TYPES = [17, 18, 33] as const, CONCLUSIONS = ["types_17_18_use_selected_enemy_hp_float_percent", "type_33_uses_runtime_selected_player_or_enemy_integer_hp_percent", "all_comparators_are_inclusive"], UNKNOWNS = ["selection_flag_semantic_name", "types_17_18_nonzero_gate_meaning", "activation_timing", "recurrence", "calculation_bucket", "types_17_18_zero_max_policy"];
function sha256(value: Buffer): string { return createHash("sha256").update(value).digest("hex"); }
function integer(value: unknown): number | undefined { const parsed = typeof value === "number" ? value : typeof value === "string" && value.trim() ? Number(value) : NaN; return Number.isSafeInteger(parsed) ? parsed : undefined; }
function relocation(inspection: NativeRuntimeElfInspection, offset: number, symbol: string): boolean { const matches = inspection.relocations.filter(value => value.offset === offset); return matches.length === 1 && matches[0].symbolName === symbol; }
function validateEvidence(inspection: NativeRuntimeElfInspection, evidence: Db27NativeTargetHpEvidence, nativeSha256: string): void {
    if (evidence.schemaVersion !== 1 || evidence.sourceSha256 !== nativeSha256 || evidence.auditScope !== "causality-types-17-18-33-target-hp-percent" || JSON.stringify(evidence.conclusions) !== JSON.stringify(CONCLUSIONS) || JSON.stringify(evidence.unknowns) !== JSON.stringify(UNKNOWNS)) throw new Error("DB27 native evidence identity mismatch");
    const v = evidence.abilityStatusVtable, vtableSymbol = inspection.symbols.find(value => value.name === v.symbol);
    if (!vtableSymbol || vtableSymbol.value !== v.vma || vtableSymbol.size !== v.sizeBytes || v.sizeBytes !== 360 || v.deckIndexSlotOffset !== 88 || v.selectionFlagSlotOffset !== 96 || v.selectionFlagSemanticName !== "unknown" || !relocation(inspection, v.vma + v.deckIndexSlotOffset, v.deckIndexSymbol) || !relocation(inspection, v.vma + v.selectionFlagSlotOffset, v.selectionFlagSymbol)) throw new Error("DB27 AbilityStatus vtable evidence mismatch");
    if (evidence.handlers.length !== 3 || new Set(evidence.handlers.map(value => value.causalityType)).size !== 3) throw new Error("DB27 handler cardinality mismatch");
    for (const handler of evidence.handlers) { const symbol = inspection.symbols.find(value => value.name === handler.symbol); if (!symbol || symbol.value !== handler.vma || symbol.size !== handler.sizeBytes || sha256(inspection.readVirtualBytes(handler.vma, handler.sizeBytes)) !== handler.codeSha256) throw new Error(`DB27 handler evidence mismatch ${handler.causalityType}`); const expected = handler.causalityType === 17 ? ["selected_enemy_current_hp_div_max_times_100_float", "gte", "selection_flag_equals_zero", "cau_val1", "cau_val2,cau_val3"] : handler.causalityType === 18 ? ["selected_enemy_current_hp_div_max_times_100_float", "lte", "selection_flag_equals_zero", "cau_val1", "cau_val2,cau_val3"] : ["runtime_selected_player_or_enemy_hp_rate_integer", "between_inclusive", "none", "cau_val1,cau_val2", "cau_val3"]; if ([handler.metric, handler.comparator, handler.gate, handler.parameterReads.join(","), handler.ignoredParameters.join(",")].join("|") !== expected.join("|")) throw new Error(`DB27 handler semantics mismatch ${handler.causalityType}`); }
    const h = evidence.hpRateHelper, symbol = inspection.symbols.find(value => value.name === h.symbol); if (!symbol || symbol.value !== h.vma || symbol.size !== h.sizeBytes || sha256(inspection.readVirtualBytes(h.vma, h.sizeBytes)) !== h.codeSha256 || h.zeroNumeratorOrDenominatorResult !== 0 || JSON.stringify(h.normalizationWindows) !== JSON.stringify([{ when: "ratio_percent_gt_0_lt_1", value: 1 }, { when: "ratio_percent_gt_99_lt_100", value: 99 }]) || h.rounding !== "nearest_ties_away_from_zero") throw new Error("DB27 HP helper evidence mismatch");
}
function collect(expression: Db11ConditionExpression): Array<{ id: string, type: 17 | 18 | 33 }> { const rawType = integer(expression.op === "unknown" ? expression.causalityType ?? (typeof expression.raw === "object" && expression.raw !== null ? (expression.raw as Record<string, unknown>).causality_type : undefined) : undefined); if (expression.op === "unknown" && TYPES.includes(rawType as 17 | 18 | 33)) return [{ id: String(expression.causalityId), type: rawType as 17 | 18 | 33 }]; if (expression.op === "not") return collect(expression.child); if (expression.op === "all" || expression.op === "any") return expression.children.flatMap(collect); return []; }
export function buildDatabaseTeamAnalysisDb27Dataset(options: { db8: DatabaseTeamAnalysisDb8Dataset, db8Sha256: string, db9: DatabaseTeamAnalysisDb9Dataset, db9Sha256: string, db11: DatabaseTeamAnalysisDb11Dataset, db11Sha256: string, tables: DatabaseExperimentTables, inspection: NativeRuntimeElfInspection, nativeSha256: string, nativeSizeBytes: number, evidence: Db27NativeTargetHpEvidence, evidenceSha256: string }): DatabaseTeamAnalysisDb27Dataset { if (options.db8.contractVersion !== "0.7.0" || options.db9.contractVersion !== "0.8.0" || options.db11.contractVersion !== "0.10.0" || options.db8.semanticPromotionCount !== 0 || options.db9.semanticPromotionCount !== 0 || options.db8.sourceSha256 !== options.db9.sourceDatabaseSha256 || options.db8.sourceSha256 !== options.db11.sourceSha256 || options.db8.sourceSnapshotVersion !== options.db9.sourceSnapshotVersion || options.db8.sourceSnapshotVersion !== options.db11.sourceSnapshotVersion || options.db9.sourceDb8.sha256 !== options.db8Sha256 || options.db9.nativeRuntime.sha256 !== options.nativeSha256 || options.db9.nativeRuntime.sizeBytes !== options.nativeSizeBytes) throw new Error("DB27 source lineage mismatch"); validateEvidence(options.inspection, options.evidence, options.nativeSha256); const handlers = new Map(options.evidence.handlers.map(value => [value.causalityType, value])); for (const type of TYPES) { const gaps = options.db8.causalityGaps.filter(value => integer(value.causalityType) === type), runtimes = options.db9.causalityGapEvidence.filter(value => value.enumValue === type), dispatches = options.db9.causalityDispatchSlots.filter(value => value.enumValue === type), handler = handlers.get(type)!; if (gaps.length !== 1 || runtimes.length !== 1 || dispatches.length !== 1) throw new Error(`DB27 evidence cardinality mismatch ${type}`); if (runtimes[0].identityStatus !== "runtime_identified" || runtimes[0].occurrenceCount !== gaps[0].occurrenceCount || runtimes[0].affectedStateCount !== gaps[0].affectedStateCount || runtimes[0].symbol !== handler.symbol || runtimes[0].symbolAddress !== handler.vma || dispatches[0].status !== "identified" || dispatches[0].symbol !== handler.symbol || dispatches[0].symbolAddress !== handler.vma) throw new Error(`DB27 runtime lineage mismatch ${type}`); }
    const rows = new Map<string, Record<string, SqliteScalar>>(); for (const row of options.tables.skill_causalities ?? []) { if (row.id === null || row.id === "") throw new Error("DB27 causality row has no id"); const id = String(row.id); if (rows.has(id)) throw new Error(`DB27 duplicate causality row ${id}`); rows.set(id, row); } const resolutions: Db27TargetHpResolution[] = [], keys = new Set<string>(); for (const state of options.db11.states) for (const rule of state.passive?.rules ?? []) for (const found of collect(rule.condition)) { const key = `${state.stateKey}|${rule.ruleKey}|${found.id}`; if (keys.has(key)) throw new Error(`DB27 duplicate occurrence ${key}`); keys.add(key); const row = rows.get(found.id), handler = handlers.get(found.type)!; if (!row || !["causality_type", "cau_val1", "cau_val2", "cau_val3"].every(column => Object.prototype.hasOwnProperty.call(row, column)) || integer(row.causality_type) !== found.type) throw new Error(`DB27 causality row missing or incomplete ${found.id}`); const v1 = integer(row.cau_val1), v2 = integer(row.cau_val2); let predicate: Db27TargetHpPredicate | undefined; if ((found.type === 17 || found.type === 18) && v1 !== undefined) predicate = { metric: "selected_enemy_hp_percent_float", comparator: found.type === 17 ? "gte" : "lte", threshold: v1, selectionGate: { rawFlagComparator: "eq", value: 0, semanticName: "unknown" }, zeroMaxPolicy: "unknown" }; if (found.type === 33 && v1 !== undefined && v2 !== undefined && v1 <= v2) predicate = { metric: "runtime_selected_hp_percent_integer", comparator: "between_inclusive", lower: v1, upper: v2, selection: { rawFlagZero: "player", rawFlagNonzero: "enemy", semanticName: "unknown" }, calculation: { zeroNumeratorOrDenominatorResult: 0, normalizationWindows: [{ when: "ratio_percent_gt_0_lt_1", value: 1 }, { when: "ratio_percent_gt_99_lt_100", value: 99 }], rounding: "nearest_ties_away_from_zero" } }; if (!predicate) throw new Error(`DB27 invalid threshold payload ${found.id}`); resolutions.push({ stateKey: state.stateKey, ruleKey: rule.ruleKey, passiveSkillId: rule.source.passiveSkillId, causalityId: found.id, causalityType: found.type, semanticStatus: "partial", predicate, raw: { cauVal1: row.cau_val1, cauVal2: row.cau_val2, cauVal3: row.cau_val3 }, activation: { timing: "unknown", recurrence: "unknown", calculationBucket: "unknown" }, provenance: { database: { table: "skill_causalities", rowId: found.id, columns: ["causality_type", "cau_val1", "cau_val2", "cau_val3"] }, runtime: { fileName: "libcocos2dcpp.so", sha256: options.nativeSha256, evidenceFile: "native-target-hp-semantics.json", evidenceSha256: options.evidenceSha256, handlerSymbol: handler.symbol, handlerVma: handler.vma, handlerSizeBytes: handler.sizeBytes, handlerCodeSha256: handler.codeSha256 } } }); }
    resolutions.sort((a, b) => a.stateKey.localeCompare(b.stateKey, "en", { numeric: true }) || a.ruleKey.localeCompare(b.ruleKey, "en", { numeric: true }) || a.causalityType - b.causalityType); for (const type of TYPES) { const gap = options.db8.causalityGaps.find(value => integer(value.causalityType) === type)!; const values = resolutions.filter(value => value.causalityType === type); if (values.length !== gap.occurrenceCount || new Set(values.map(value => value.stateKey)).size !== gap.affectedStateCount || new Set(values.map(value => value.causalityId)).size !== gap.uniqueCausalityCount) throw new Error(`DB27 source gap accounting mismatch ${type}`); } return { schemaVersion: 1, contract: "dokkan-team-analysis-target-hp-native-semantics-experiment", contractVersion: "0.26.0", generatedAt: options.db11.generatedAt, sourceSnapshotVersion: options.db8.sourceSnapshotVersion, sourceDatabaseSha256: options.db8.sourceSha256, sourceDb8: { fileName: "team-analysis-db8-evidence-experiment.json.gz", sha256: options.db8Sha256, contractVersion: "0.7.0" }, sourceDb9: { fileName: "team-analysis-db9-runtime-evidence.json.gz", sha256: options.db9Sha256, contractVersion: "0.8.0" }, sourceDb11: { fileName: "team-analysis-db11-experiment.json.gz", sha256: options.db11Sha256, contractVersion: "0.10.0" }, nativeRuntime: { fileName: "libcocos2dcpp.so", sha256: options.nativeSha256, sizeBytes: options.nativeSizeBytes }, nativeEvidence: { fileName: "native-target-hp-semantics.json", sha256: options.evidenceSha256 }, inheritedSemanticPromotionCount: 3, semanticPromotionCount: 3, resolutions }; }
export function buildDatabaseTeamAnalysisDb27Coverage(dataset: DatabaseTeamAnalysisDb27Dataset, sourceGapOccurrenceCount: number): DatabaseTeamAnalysisDb27Coverage { const counts: Record<string, number> = {}, states: Record<string, number> = {}; for (const type of TYPES) { const values = dataset.resolutions.filter(value => value.causalityType === type); counts[String(type)] = values.length; states[String(type)] = new Set(values.map(value => value.stateKey)).size; } return { schemaVersion: 1, sourceGapOccurrenceCount, resolutionCount: dataset.resolutions.length, affectedStateCount: new Set(dataset.resolutions.map(value => value.stateKey)).size, occurrenceCountsByType: counts, affectedStateCountsByType: states, partialResolutionCount: dataset.resolutions.length, invalidThresholdCount: 0, inheritedSemanticPromotionCount: 3, semanticPromotionCount: 3 }; }
