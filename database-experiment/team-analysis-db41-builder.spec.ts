import{equal,throws}from"assert";import{existsSync,readFileSync}from"fs";import{resolve}from"path";import{evaluateDb41EnemyCategoryThreshold,validateDb41NativeEvidence}from"./team-analysis-db41-builder";
const p=existsSync(resolve(__dirname,"native-enemy-category-count-threshold-semantics.json"))?resolve(__dirname,"native-enemy-category-count-threshold-semantics.json"):resolve(__dirname,"..","..","database-experiment","native-enemy-category-count-threshold-semantics.json"),e=JSON.parse(readFileSync(p,"utf8"));
describe("database Team Analysis DB41 enemy category count",()=>{it("uses the supported enemy mode and unsigned threshold comparison",()=>{equal(evaluateDb41EnemyCategoryThreshold(1,0,1),false);equal(evaluateDb41EnemyCategoryThreshold(1,1,1),true);equal(evaluateDb41EnemyCategoryThreshold(1,0,0),true);equal(evaluateDb41EnemyCategoryThreshold(1,10,-1),false)});it("keeps unsupported modes and invalid payloads unknown",()=>{equal(evaluateDb41EnemyCategoryThreshold(0,1,1),null);equal(evaluateDb41EnemyCategoryThreshold(2,1,1),null);equal(evaluateDb41EnemyCategoryThreshold(1,-1,1),null);equal(evaluateDb41EnemyCategoryThreshold(1,1,"bad"),null)});it("rejects substituted native evidence",()=>{const x=JSON.parse(JSON.stringify(e));x.threshold.comparison="signed_gte";throws(()=>validateDb41NativeEvidence({}as any,x,e.sourceSha256),/identity/);const y=JSON.parse(JSON.stringify(e));y.codeRegions[2].codeSha256="0".repeat(64);throws(()=>validateDb41NativeEvidence({readVirtualBytes:()=>Buffer.alloc(756),symbols:[],relocations:[]}as any,y,e.sourceSha256),/region/)})});
