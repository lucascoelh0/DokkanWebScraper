import { DatabaseExperimentTables } from "./builder";
import { SqliteScalar } from "./contract";
import { DatabaseTeamAnalysisDb11Dataset } from "./team-analysis-db11-contract";
import { DatabaseTeamAnalysisDb31Dataset } from "./team-analysis-db31-contract";
import { DatabaseTeamAnalysisDb35Dataset } from "./team-analysis-db35-contract";
import { DatabaseTeamAnalysisDb36Dataset } from "./team-analysis-db36-contract";
import { DatabaseTeamAnalysisDb37Dataset } from "./team-analysis-db37-contract";
import { db39OperandUnit, projectDb39Bucket } from "./team-analysis-db39-builder";
import { projectDb44Modifier } from "./team-analysis-db44-builder";
import { DatabaseTeamAnalysisDb44Dataset, Db44NativeEvidence } from "./team-analysis-db44-contract";

export interface Db44Validation { schemaVersion: 1; valid: boolean; ruleCount: number; losslessReconstructionCount: number; failures: string[] }
export interface Db44ValidationContext { db8Sha256: string; db9Sha256: string; db11Sha256: string; db31Sha256: string; db34Sha256: string; db35Sha256: string; db36Sha256: string; db37Sha256: string; db39Sha256: string; db39NativeEvidenceSha256: string; nativeSha256: string; nativeSizeBytes: number; evidence: Db44NativeEvidence; evidenceSha256: string }
const integer = (value: unknown): number | undefined => { const parsed = typeof value === "number" ? value : typeof value === "string" && value.trim() ? Number(value) : NaN; return Number.isSafeInteger(parsed) ? parsed : undefined; };
const key = (value: { stateKey: string; ruleKey: string }) => `${value.stateKey}|${value.ruleKey}`;
const index = <T extends { stateKey: string; ruleKey: string }>(values: T[]) => new Map(values.map(value => [key(value), value]));
const COLUMNS = ["efficacy_type", "eff_value1", "eff_value2", "eff_value3", "calc_option", "exec_timing_type", "target_type", "sub_target_type_set_id", "turn", "is_once"];
const COUNT_INPUT = { status: "partial", source: "InGameCharaData.getObtainedBallTypeNumbers", rawBallType: 11, rawBitpattern: 0, absentMapEntryValue: 0, semanticName: "unknown" };
const INDEPENDENT_DIMENSIONS = { condition: "independent", target: "inherited_db35_db36", timing: "independent", calculationOperation: "inherited_db31", calculationBucket: "inherited_db34", duration: "inherited_db37", onceOnly: "inherited_db37", recurrence: "partial", reset: "unknown", stacking: "unknown" };

export function hasDb44ConservativeBoundaries(rule: Pick<DatabaseTeamAnalysisDb44Dataset["ruleProjections"][number], "countInput" | "independentDimensions">): boolean {
    return JSON.stringify(rule.countInput) === JSON.stringify(COUNT_INPUT) && JSON.stringify(rule.independentDimensions) === JSON.stringify(INDEPENDENT_DIMENSIONS);
}

export function validateDatabaseTeamAnalysisDb44Dataset(dataset: DatabaseTeamAnalysisDb44Dataset, db11: DatabaseTeamAnalysisDb11Dataset, db31: DatabaseTeamAnalysisDb31Dataset, db35: DatabaseTeamAnalysisDb35Dataset, db36: DatabaseTeamAnalysisDb36Dataset, db37: DatabaseTeamAnalysisDb37Dataset, tables: DatabaseExperimentTables, context: Db44ValidationContext): Db44Validation {
    const failures: string[] = [];
    if (dataset.schemaVersion !== 1 || dataset.contract !== "dokkan-team-analysis-single-stat-energy-ball-proportional-native-semantics-experiment" || dataset.contractVersion !== "0.43.0" || dataset.generatedAt !== db37.generatedAt || dataset.sourceSnapshotVersion !== db37.sourceSnapshotVersion || dataset.sourceDatabaseSha256 !== db37.sourceDatabaseSha256 || dataset.inheritedSemanticPromotionCount !== 51 || dataset.semanticPromotionCount !== 2) failures.push("dataset identity");
    const expectedLineage = {
        sourceDb8: { fileName: "team-analysis-db8-evidence-experiment.json.gz", sha256: context.db8Sha256, contractVersion: "0.7.0" }, sourceDb9: { fileName: "team-analysis-db9-runtime-evidence.json.gz", sha256: context.db9Sha256, contractVersion: "0.8.0" }, sourceDb11: { fileName: "team-analysis-db11-experiment.json.gz", sha256: context.db11Sha256, contractVersion: "0.10.0" }, sourceDb31: { fileName: "team-analysis-db31-skill-calc-option.json.gz", sha256: context.db31Sha256, contractVersion: "0.30.0" }, sourceDb34: { fileName: "team-analysis-db34-basic-stat-buckets.json.gz", sha256: context.db34Sha256, contractVersion: "0.33.0" }, sourceDb35: { fileName: "team-analysis-db35-target-dispatch.json.gz", sha256: context.db35Sha256, contractVersion: "0.34.0" }, sourceDb36: { fileName: "team-analysis-db36-sub-target-semantics.json.gz", sha256: context.db36Sha256, contractVersion: "0.35.0" }, sourceDb37: { fileName: "team-analysis-db37-passive-lifecycle.json.gz", sha256: context.db37Sha256, contractVersion: "0.36.0" }, sourceDb39: { fileName: "team-analysis-db39-energy-ball-proportional-stats.json.gz", sha256: context.db39Sha256, contractVersion: "0.38.0", nativeEvidenceSha256: context.db39NativeEvidenceSha256 }, nativeRuntime: { fileName: "libcocos2dcpp.so", sha256: context.nativeSha256, sizeBytes: context.nativeSizeBytes }, nativeEvidence: { fileName: "native-single-stat-energy-ball-proportional-semantics.json", sha256: context.evidenceSha256 },
    };
    if (JSON.stringify({ sourceDb8: dataset.sourceDb8, sourceDb9: dataset.sourceDb9, sourceDb11: dataset.sourceDb11, sourceDb31: dataset.sourceDb31, sourceDb34: dataset.sourceDb34, sourceDb35: dataset.sourceDb35, sourceDb36: dataset.sourceDb36, sourceDb37: dataset.sourceDb37, sourceDb39: dataset.sourceDb39, nativeRuntime: dataset.nativeRuntime, nativeEvidence: dataset.nativeEvidence }) !== JSON.stringify(expectedLineage)) failures.push("dataset lineage");
    const source = new Map<string, { passiveSkillId: string; effectCount: number; currentKind: string | null }>(); for (const state of db11.states) for (const rule of state.passive?.rules ?? []) if ([59, 60].includes(integer(rule.source.efficacyType) ?? -1)) source.set(key({ stateKey: state.stateKey, ruleKey: rule.ruleKey }), { passiveSkillId: rule.source.passiveSkillId, effectCount: rule.effects.length, currentKind: rule.effects[0]?.kind ?? null });
    const rows = new Map<string, Record<string, SqliteScalar>>((tables.passive_skills ?? []).map(row => [String(row.id), row])), operations = index(db31.ruleProjections), targets = index(db35.ruleTargets), subTargets = index(db36.ruleSubTargets), lifecycles = index(db37.ruleLifecycles), seen = new Set<string>(); let losslessReconstructionCount = 0;
    const proofRoles = [...context.evidence.codeRegions.map(value => value.role), ...(context.evidence.sharedConsumerEvidence.inheritedProofRoles as string[]).map(value => `db39:${value}`)], inherited = { db8Sha256: context.db8Sha256, db9Sha256: context.db9Sha256, db11Sha256: context.db11Sha256, db31Sha256: context.db31Sha256, db34Sha256: context.db34Sha256, db35Sha256: context.db35Sha256, db36Sha256: context.db36Sha256, db37Sha256: context.db37Sha256, db39Sha256: context.db39Sha256, db39NativeEvidenceSha256: context.db39NativeEvidenceSha256 };
    for (const rule of dataset.ruleProjections) {
        const k = key(rule), expected = source.get(k), row = rows.get(rule.passiveSkillId), operation = operations.get(k), target = targets.get(k), subTarget = subTargets.get(k), lifecycle = lifecycles.get(k);
        if (seen.has(k)) failures.push(`duplicate ${k}`); seen.add(k);
        if (!expected || !row || !operation || !target || !subTarget || !lifecycle || expected.passiveSkillId !== rule.passiveSkillId || expected.effectCount !== rule.sourceEffectCount || operation.passiveSkillId !== rule.passiveSkillId || target.passiveSkillId !== rule.passiveSkillId || subTarget.passiveSkillId !== rule.passiveSkillId || lifecycle.passiveSkillId !== rule.passiveSkillId || integer(row.efficacy_type) !== rule.efficacyType) { failures.push(`source identity ${k}`); continue; }
        const rawTuple = [row.eff_value1, row.eff_value2, row.eff_value3, row.calc_option, row.exec_timing_type, row.target_type, row.sub_target_type_set_id, row.turn, row.is_once], projectedTuple = [rule.statModifier.rawModifierPerCount, rule.ignoredBehavioralParameters.rawEffValue2, rule.ignoredBehavioralParameters.rawEffValue3, rule.rawCalculationOption, rule.rawExecutionTimingType, rule.rawTargetType, rule.target.subTarget.rawSetId, rule.lifecycle.duration.rawTurn, rule.lifecycle.onceOnly.rawIsOnce];
        if (JSON.stringify(rawTuple) !== JSON.stringify(projectedTuple)) failures.push(`lossless sqlite reconstruction ${k}`); else losslessReconstructionCount += 1;
        if (JSON.stringify(rule.statModifier) !== JSON.stringify(projectDb44Modifier(row.efficacy_type, row.eff_value1)) || JSON.stringify(rule.calculationBucket) !== JSON.stringify(projectDb39Bucket(row.exec_timing_type)) || JSON.stringify(rule.calculationOperation) !== JSON.stringify(operation.operation) || JSON.stringify(rule.operandUnit) !== JSON.stringify(db39OperandUnit(operation.operation))) failures.push(`stat projection ${k}`);
        if (target.target.status !== "supported" || rule.target.status !== "supported" || JSON.stringify(rule.target.candidate) !== JSON.stringify(target.target.value) || subTarget.status !== "supported" || JSON.stringify(rule.target.subTarget) !== JSON.stringify({ status: "supported", rawSetId: subTarget.rawSetId, composition: subTarget.composition, emptySetBehavior: subTarget.emptySetBehavior, filters: subTarget.filters })) failures.push(`target projection ${k}`);
        if (JSON.stringify(rule.lifecycle.duration) !== JSON.stringify(lifecycle.duration) || JSON.stringify(rule.lifecycle.onceOnly) !== JSON.stringify(lifecycle.onceOnly) || rule.lifecycle.recurrence !== "partial" || rule.lifecycle.resetAndExpiry !== "unknown") failures.push(`lifecycle ${k}`);
        const expectedStatus = rule.statModifier.status === "supported" && rule.calculationBucket.status === "supported" && rule.calculationOperation.status === "supported" ? "partial" : "unknown";
        if (rule.statModifier.sourceColumn !== "eff_value1" || rule.ignoredBehavioralParameters.status !== "not_read_by_handler" || !hasDb44ConservativeBoundaries(rule) || rule.simulationStatus !== expectedStatus || rule.legacyComparison.currentKind !== expected.currentKind || rule.legacyComparison.status !== (expected.currentKind === "unknown" ? "representation_gain_legacy_unknown" : "not_comparable") || rule.legacyComparison.confirmedConflict !== false) failures.push(`semantic boundary ${k}`);
        const runtime = { fileName: "libcocos2dcpp.so", sha256: context.nativeSha256, evidenceFile: "native-single-stat-energy-ball-proportional-semantics.json", evidenceSha256: context.evidenceSha256, proofRoles };
        if (JSON.stringify(rule.provenance.database) !== JSON.stringify({ table: "passive_skills", rowId: rule.passiveSkillId, columns: COLUMNS }) || JSON.stringify(rule.provenance.runtime) !== JSON.stringify(runtime) || JSON.stringify(rule.provenance.inherited) !== JSON.stringify(inherited)) failures.push(`provenance ${k}`);
    }
    if (source.size !== 33 || dataset.ruleProjections.length !== source.size || seen.size !== source.size || [...source.keys()].some(value => !seen.has(value))) failures.push(`cardinality ${dataset.ruleProjections.length}/${source.size}`);
    return { schemaVersion: 1, valid: failures.length === 0, ruleCount: dataset.ruleProjections.length, losslessReconstructionCount, failures };
}
