import { createHash } from "crypto";
import { execFileSync } from "child_process";
import { lstatSync, readFileSync, realpathSync, statSync } from "fs";
import { relative, resolve } from "path";
import { SpecialM3Inputs } from "./special-m3-joins";
import { SpecialM3SourceLock } from "./special-m3-contract";
function sha256(value: string): string { return createHash("sha256").update(value).digest("hex"); }
function safeRead(root: string, path: string, size: number, hash: string): string { if (!/^[A-Za-z0-9_.\/-]+$/.test(path) || path.includes("..") || path.startsWith("/") || /^[A-Za-z]:/.test(path)) throw new Error("M3 unsafe source path"); const rootReal = realpathSync(root), candidate = resolve(rootReal, path), inside = relative(rootReal, candidate); if (!inside || inside.startsWith("..") || resolve(rootReal, inside) !== candidate) throw new Error("M3 source escaped root"); let cursor = rootReal; for (const part of inside.split(/[\\/]/)) { cursor = resolve(cursor, part); if (lstatSync(cursor).isSymbolicLink()) throw new Error("M3 source path contains a link"); } const before = statSync(candidate); if (!before.isFile() || before.size !== size) throw new Error("M3 source size mismatch"); const text = readFileSync(candidate, "utf8"), after = statSync(candidate); if (before.size !== after.size || before.mtimeMs !== after.mtimeMs || Buffer.byteLength(text) !== size || sha256(text) !== hash) throw new Error("M3 source identity mismatch"); return text; }
export function loadSpecialM3Sources(root: string, databaseRoot: string, lock: SpecialM3SourceLock): SpecialM3Inputs { if (lock.schemaVersion !== 1 || lock.contract !== "dokkan-special-modes-database-source-lock" || lock.contractVersion !== "0.4.1" || JSON.stringify(lock.artifacts.map(value => value.key).sort()) !== JSON.stringify(["e1", "e2", "e4", "s2"])) throw new Error("M3 source lock mismatch"); const payloads = new Map<string, any>(), lineage: SpecialM3Inputs["lineage"] = []; for (const item of lock.artifacts) { const payloadText = safeRead(root, item.payloadPath, item.payloadSizeBytes, item.payloadSha256), manifestText = safeRead(root, item.manifestPath, item.manifestSizeBytes, item.manifestSha256), validationText = safeRead(root, item.validationPath, item.validationSizeBytes, item.validationSha256), payload = JSON.parse(payloadText), manifest = JSON.parse(manifestText), validation = JSON.parse(validationText); if (payload.contract !== item.contractName || payload.contractVersion !== item.contractVersion || manifest.contractVersion !== item.contractVersion || manifest.fileName !== item.payloadPath.split("/").at(-1) || manifest.sizeBytes !== item.payloadSizeBytes || manifest.sha256 !== item.payloadSha256 || validation.valid !== true) throw new Error(`M3 source contract mismatch ${item.key}`); payloads.set(item.key, payload); lineage.push({ key: item.key, sourceKind: "validated_artifact", contract: item.contractName, contractVersion: item.contractVersion, payloadSizeBytes: item.payloadSizeBytes, payloadSha256: item.payloadSha256, manifestSha256: item.manifestSha256, validationSha256: item.validationSha256 }); } const databasePath = resolve(realpathSync(databaseRoot), lock.database.fileName); if (relative(realpathSync(databaseRoot), databasePath).startsWith("..") || lstatSync(databasePath).isSymbolicLink()) throw new Error("M3 database path boundary"); const before = readFileSync(databasePath); if (before.length !== lock.database.sizeBytes || createHash("sha256").update(before).digest("hex") !== lock.database.sha256) throw new Error("M3 database identity mismatch"); const bridge = resolve(process.cwd(), "database-special-modes-captures", "special-m3-sqlite-readonly-bridge.py"), sqlite = JSON.parse(execFileSync(process.platform === "win32" ? "python" : "python3", [bridge, "--database", databasePath], { encoding: "utf8", maxBuffer: 1024 * 1024, windowsHide: true })); const after = readFileSync(databasePath); if (after.length !== before.length || createHash("sha256").update(after).digest("hex") !== lock.database.sha256 || sqlite.contract !== "dokkan-special-modes-sqlite-structural-evidence" || sqlite.contractVersion !== "0.4.1" || sqlite.collectionMode !== "sqlite_uri_mode_ro_query_only") throw new Error("M3 database read-only receipt failed"); lineage.push({ key: "sqlite", sourceKind: "sqlite_snapshot", contract: sqlite.contract, contractVersion: sqlite.contractVersion, payloadSizeBytes: lock.database.sizeBytes, payloadSha256: lock.database.sha256, manifestSha256: null, validationSha256: null }); return { e1: payloads.get("e1"), e2: payloads.get("e2"), e4: payloads.get("e4"), s2: payloads.get("s2"), sqlite, lineage }; }
