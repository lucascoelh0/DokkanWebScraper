import { mkdirSync, readFileSync, writeFileSync } from "fs"; import { resolve } from "path"; import { getHeapStatistics } from "v8"; import { CaptureInputManifest } from "./capture-h0-contract"; import { validateCaptureH8 } from "./capture-h8-audit"; import { CaptureH8Dataset } from "./capture-h8-contract"; import { buildCaptureH9, validateCaptureH9 } from "./capture-h9-schemas"; import { collectCaptureSensitiveValues, scanTextsForSecrets } from "./capture-secret-scan";
const root=resolve(process.cwd()); if(getHeapStatistics().heap_size_limit>=1024*1024*1024) throw new Error("H9 requires Node heap below 1 GiB"); const manifest=JSON.parse(readFileSync(resolve(root,"database-server-captures/capture-h8-input-manifest.json"),"utf8")) as CaptureInputManifest, roots={"dokkan-local-captures-0810":"D:\\Dokkan\\har logs\\08-10"}; const h8Text=readFileSync(resolve(root,"data/database-server-captures/h8/capture-h8-inventory.json"),"utf8"), h8=JSON.parse(h8Text) as CaptureH8Dataset; if(!validateCaptureH8(h8,manifest,roots).valid) throw new Error("H9 requires validated H8 lineage"); const dataset=buildCaptureH9(manifest,roots,h8,h8Text), validation=validateCaptureH9(dataset,manifest,roots,h8,h8Text), text=`${JSON.stringify(dataset,null,2)}\n`, scan=scanTextsForSecrets(collectCaptureSensitiveValues(manifest,roots),[{name:"capture-h9-observed-schemas.json",text}]); if(!scan.valid) throw new Error("H9 secret scan failed"); const out=resolve(root,"data/database-server-captures/h9"); mkdirSync(out,{recursive:true}); writeFileSync(resolve(out,"capture-h9-observed-schemas.json"),text); writeFileSync(resolve(out,"capture-h9-validation.json"),`${JSON.stringify(validation,null,2)}\n`); writeFileSync(resolve(out,"capture-h9-secret-scan.json"),`${JSON.stringify(scan,null,2)}\n`); process.stdout.write(`${JSON.stringify({...validation,secretScanValid:scan.valid})}\n`);
